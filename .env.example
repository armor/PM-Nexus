# =============================================================================
# UI-UPLIFT ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit your .env file - it contains secrets!
# =============================================================================

# -----------------------------------------------------------------------------
# JIRA CONFIGURATION
# -----------------------------------------------------------------------------

# Your Atlassian site URL (without https://)
# Example: armor-defense.atlassian.net
JIRA_HOST=your-company.atlassian.net

# Your Atlassian account email
# This must match your Atlassian login email
JIRA_EMAIL=your.email@company.com

# Jira API Token (see instructions below)
JIRA_API_TOKEN=your-api-token-here

# Default Jira project for sync operations
# This is the project key (e.g., STAGE, PROJ, DEV)
JIRA_DEFAULT_PROJECT=STAGE

# -----------------------------------------------------------------------------
# HOW TO GET YOUR JIRA API TOKEN
# -----------------------------------------------------------------------------
#
# 1. Go to: https://id.atlassian.com/manage-profile/security/api-tokens
#
# 2. Click "Create API token"
#
# 3. Give it a name like "Claude Code - UI-Uplift"
#
# 4. Click "Create"
#
# 5. IMPORTANT: Copy the token immediately!
#    You won't be able to see it again.
#
# 6. Paste the token as the JIRA_API_TOKEN value above
#
# Note: API tokens inherit your account's permissions.
#       Make sure your account has access to the projects you need.
#
# Troubleshooting:
#   - "401 Unauthorized": Token expired or email doesn't match
#   - "403 Forbidden": Your account lacks project permissions
#   - Regenerate token if issues persist
#
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# OPTIONAL: DEBUG MODE
# -----------------------------------------------------------------------------

# Enable verbose logging for troubleshooting
# Set to "true" to see API requests in stderr
DEBUG=false

# -----------------------------------------------------------------------------
# CONFLUENCE CONFIGURATION (uses same credentials as Jira)
# -----------------------------------------------------------------------------
# Confluence uses the same JIRA_HOST, JIRA_EMAIL, and JIRA_API_TOKEN
# No additional configuration needed!
#
# Your Confluence URL will be: https://{JIRA_HOST}/wiki
# -----------------------------------------------------------------------------
